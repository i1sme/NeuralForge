[package]
name = "compiler"
version = "0.1.0"
edition = "2021"
description = "NeuralForge compiler core — lexer, parser, UIR builder (lib only)"
license.workspace = true

[dependencies]

[lib]
path = "src/lib.rs"
